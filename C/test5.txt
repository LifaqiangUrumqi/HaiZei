1
3.0分
以下程序的输出结果是( )。





#include <stdio.h>

#include <string.h>

int main() {

    char st[20]="hello\0\t\\";

    printf("%d %d\n", strlen(st), sizeof(st));

}


 


A.
9  9
B.
5  20
C.
13  20
D.
20  20
正确答案是 B， 回答正确  收藏 
计算字符串的长度是从第一个字符开始一直计数到第一个’\0’为止（不包含

‘\0’)，对于st存放的字符串，第6个元素就是’\0’，所以字符串的长度是5，但数组st的大

小与赋值的字符串无关，是其定义时的大小20。所以正确答案是B。

2
2.0分
下列程序的输出结果是（）。





main() { 

    int x = 1, y = 0, a = 0, b = 0;

    switch(x) {

        case 1:switch(y) {

            case 0:a++;break;

            case 1:b++;break;

        }

        case 2:a++;b++;break;

        case 3:a++;b++;break;

    }

    printf("a=%d,b=%d\n",a,b);

}


 


A.
a=1,b=0
B.
a=2,b=1
C.
a=1,b=1
D.
a=2,b=2
正确答案是 B， 你的答案是 A。你答错了  收藏 
case 1分支没有break，所以接着运行case 2后面的分支。

3
2.0分
以下对枚举类型名的定义中正确的是（）。


A.
enum a={sum,mon,tue};
B.
enum a {sum=9,mon=-1,tue};
C.
enum a={"sum","mon","tue"};
D.
enum a {"sum","mon","tue"};
正确答案是 B， 回答正确  收藏 
把枚举类型名看做是一个命名空间，命名空间内部的成员变量的使用相当于类成员使用规则

即类定义   class A{int a; float b;};

枚举类型定义：  enum a{x,y=8,z};  

枚举类型共享存储空间，即sizeof（a） = 4

第一个值未赋值，则默认为0，后面一个值未赋值默认为前一个值加1

则上述定义中，  a::x的值是0   a::y的值是8，   a::z的值是9

4
2.0分
在 C 语言中，能代表逻辑值“真”的是（）。


A.
True
B.
大于0的数
C.
非0整数
D.
非0的数
正确答案是 D， 你的答案是 C。你答错了  收藏 
C99 标准以后，C 语言中引入了 bool 类型，被定义在 stdbool.h 头文件中。包含（true）真，（false）假。

详情请见：【C 语言中 bool 类型的支持库】

5
3.0分


int main() {

    unsigned char i = 1;

    i -= 4;

    printf("the value of i is: %d", i);

}


请问i的值是：


A.
253

B.
254

C.
255

D.
256

正确答案是 A， 回答正确  收藏 
在计算 i-=4这一步时，会发生如下转换：

实际计算的是 i+(-4)的计算。

i=1     ：0000 0001

-4作为负数，在计算机中是以补码的形式参与运算。

[ -4 ]原=1000 0100

[ -4 ] 反 =1111 1011

[ -4 ] 补 =1111 1100

 

在计算时，以下两个值进行相加：

i=1:     0000 0001

-4：    1111 1100

相加之和是：1111 1101

即：253

详细见【char和unsigned char强制转换成int后的差异】

6
2.0分
以下程序的输出结果是（）。





int main(void) {

    int m[][3]={1,4,7,2,5,8,3,6,9};

    int i,j,k=2;

    for(i=0;i<3;i++) {

        printf("%d ",m[k][i]);

    }

}


 


A.
4 5 6
B.
2 5 8
C.
3 6 9
D.
7 8 9
正确答案是 C， 你的答案是 D。你答错了  收藏 
定义三列矩阵，取其第三行。

7
2.0分
下面程序的运行结果是：





#define N 2

#define M N+1

#define NUM (M+1)*M/2

main() {

    printf("%d\n",NUM);

}


 


A.
4

B.
9

C.
8

D.
6

正确答案是 C， 你的答案是 D。你答错了  收藏 
宏定义只是简单的文本替换，(2+1+1)*2+1/2=8

8
2.0分
设a、b、c、d、m、n均为int型变量，且a=5、b=6、c=7、d=8、m=2、n=2,则逻辑表达式（m=a>b）&&(n=c>d)运算后，n的值为：


A.
0

B.
1

C.
2

D.
7

正确答案是 C， 你的答案是 A。你答错了  收藏 
m=a>b后m=0，表达式为假，&&后半部分不会操作，因此n为初始值2.

逻辑与或者逻辑或的表达式，先是判断一边，若一边可以判断整个表达式为真假时，另一边不再执行。

9
2.0分
C语言中double 类型数据占字节数为（）。


A.
4

B.
8

C.
12

D.
16

正确答案是 B， 你的答案是 D。你答错了  收藏 
详情见【在C语言中，double、long、unsigned、int、char类型数据所占字节数】

10
3.0分
若有定义语句： int year=1009 ， *p=&year ；以下不能使变量 year 中的值增至 1010 的语句是


A.
*p+=1;
B.
(*p)++;
C.
++(*p)
D.
*p++
正确答案是 D， 你的答案是 C。你答错了  收藏 
*p++是先取出*p的值，然后让p++ (*p)++是先取出*p的值，让这个值++

判断题 共1题，共2分
11
2.0分
在 C 语言中，此定义和语句是合法的：enum aa{ a=5,b,c}bb;bb=(enum aa)5;请问这句话的说法是正确的吗？


正确答案是 正确 ， 你答对了  收藏 
enum aa{a=5,b,c} bb;是C语言中定义的枚举类型;意思是：定义aa这个数据类型，其取值范围是a,b,c三个数，其中，a=5, b=6,c=7(如果不给b,c指定数值，就是其前一个数+1)。
同时定义aa 这个数据类型的变量bbbb=(enum aa)5；和你学到的 int a = (int)b;功能一样，是把数值5转义成(enum aa)数据类型，同时赋值给变量bb;
